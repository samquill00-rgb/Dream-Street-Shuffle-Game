# LibreOffice / OpenOffice lock files (created while a doc is open)
.~lock.*#

# Compile-script backup snapshots (sync_html.py creates these on every run)
BACKUP-*.twee
BACKUP-*.html

# macOS metadata
.DS_Store

# Editor swap files
*.swp
*~
