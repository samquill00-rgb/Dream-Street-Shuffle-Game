# Polish loop, 2026-09-23 — running log

Brief: the seven passes Sam approved in the project chat at 04:31 UTC ("Go" at 04:32). Branch `claude/polish-loop-2026-09-23-dkhohi` off main 90e8e79. One commit per pass. Runs until about 08:35 UTC or until usage runs out. His prose, verse, pink lines, link wording, variables, endings, audio and the critic scene are untouched throughout.

Base check at start: main already carries the Third Pillar water and the Centre Point chain fixes. `claude/preview-launcher-ltc3uy` (the `?start=` links and preview.html) is NOT merged, so jumps here use `#dss-debug-jump=` / the audit harness's Start rewrite.

Rig: `python3 -m http.server 8777` from the project folder; `pip install playwright`; Chromium at `/opt/pw-browsers/chromium-1194`; node at `/opt/node22/bin/node`. Scripts in `scratchpad/polish-loop-2026-09-23/` (run from `scratchpad/audit-2026-09-16` with `PYTHONPATH=.` so they can import `harness.py`). Safari was not run; WebKit reasoning noted where it matters.

## Pass 1 — overlapping text (started 04:38 UTC, sweep running in the background)
- New `overlap_sweep.py`: renders every story passage at 1212×900 and 390×780 under the rich seed, waits 3.5s for staggered reveals, then reports (a) text runs from different elements whose boxes intersect by more than 3px both ways, (b) text runs that pass their block container's edge, (c) elements with overflow hidden whose text is taller/wider than the box. Both widths run in parallel; about 30s a passage under software GL.
- Results below when the sweep finishes.

## Pass 2 — notebook map positions (04:38–04:44 UTC) — DONE, eight markers moved
- Reference: the Dean Street tile map's DOORS table (UserScript ~line 4179: Wardour cols 5–6, Dean 17–18, Frith 29–30, Greek 41–42; Bateman rows 15–16, Old Compton 27–28, Romilly 35–36, Shaftesbury 41–42; "every venue sits where it really sits in Soho"). The notebook map is the SVG built in `header header` (~line 51778, viewBox 560×700; Wardour x100, Dean x230, Frith x360, Greek x490; Bateman y370, Old Compton y520, Shaftesbury y620).
- Moved (old → new, viewBox units):
  - **Chippy** 100,250 → 216,228: it was on Wardour Street; the tile map has it on the west side of Dean Street in the north block (col 15, row 6).
  - **Trisha's** 494,262 → 476,404: it was on the east side of Greek Street *above* Bateman; the tile map (and 57 Greek Street) has it on the west side just *below* Bateman (col 39, row 19). Its label now sits to the right of the marker so it does not crowd Lackland's.
  - **The Ginger Light** 230,370 → 241,500: it sat on the Dean/Bateman crossing; the tile map has it on the east corner of Dean and Old Compton (col 19, row 26).
  - **Colony Room** 232,420 → 221,420: west side of Dean, not in the road (col 15).
  - **Lackland's Offices** 364,430 → 374,422: east pavement of Frith (col 32, row 21).
  - **The French House** 248,566 → 248,548: a few doors south of Old Compton (row 30), not mid-block.
  - **Coach & Horses** 490,560 → 499,560: east side of Greek (col 44).
  - **Pillars of Hercules** 486,205 → 499,205: east side of Greek (col 44).
  - Ronnie Scott's (340,450, west of Frith between Bateman and Old Compton) was already right and did not move.
- The gathered-lily markers and the five-lily pentangle share these coordinates, so they follow: the pentagram polygon is now `280,70 499,560 216,228 476,404 248,548` (Centre Point, Coach, Chippy, Trisha's, French) instead of `280,70 490,560 100,250 494,262 248,566`.
- **For Sam's eye:** with the Chippy and Trisha's where the tile map puts them, the pentangle is no longer a symmetrical star (`pass2-nbmap-before-pentangle.png` vs `pass2-nbmap-after-pentangle.png`). The old coordinates seem to have been chosen for the star rather than for Soho. If the star matters more than the map, the two lines to revert are the Chippy (`translate(216,228)` → `translate(100,250)`, 5 places) and Trisha's (`translate(476,404)` → `translate(494,262)`, 4 places) plus the two polygon `points=` lists.
- **Also for Sam:** the Lackland's approach caption reads "LACKLAND'S OFFICE, WARDOUR STREET, SOHO" (UserScript ~line 26134) while both maps put the office on Frith Street. Left alone; his call which is right.
- Screenshots in the project files: `pass2-nbmap-before-desktop.png`, `pass2-nbmap-after-desktop.png`, `pass2-nbmap-after-phone.png`, `pass2-nbmap-before-pentangle.png`, `pass2-nbmap-after-pentangle.png`. Tool: `nbmap_shot.py` (opens the notebook, switches to MAP, screenshots `.soho-map`; `SEED=` env adds Harlowe sets).
- **Verified:** map renders with 0 JS errors at 1212 and 390; the pentangle state renders with 0 errors. Changed: the map SVG in `header header` only (coordinates and three Trisha's label attributes). No words changed.

## Pass 3 — placeholder prose from the dream-loop documents (04:44–04:49 UTC) — DONE, 50 pink groups cut
- Source of the short versions: `.notes/dream-loops-build/cuts.py` in the project files (the exact text the second-edition docx files carry, keyed by passage, one entry per run of pink prose). New `apply_cuts.py` maps each entry onto the matching `<div class="claude-draft">` prose run in the twee (links inside the divs are left where they are), checks the cut introduces no more than a handful of words the long version did not have, keeps any HTML comment that was inside the div, and writes the cut in place. The pink class stays, so everything still reads as draft.
- Applied to 49 groups by the script across 37 passages (Nazca, Easter Island, Pyramid, Ezekiel loops; Third Pillar Portal, The Synthesis, The Sanctum, The Sanctum — Sitting, Alt-Dawn; the five Recognises passages; the Critic Hears the Mantra), plus one by hand: the first block of The Centre — Nazca, which carries the `(if: $nazcaRaceWon)` hook; the cut's "[only if you won the race: …][otherwise: …]" went back in as the same hook with the shortened lines.
- Left alone: The Mountain, The Cave and Himalayan Return already carry Sam's own prose (no pink left, so the docs' cut versions of the old drafts are moot). Nazca Race, Pyramid Run and The Climb have no pink prose in the game at all (only the hidden testing skip links), though the docs show three short pink paragraphs for each; nothing was added, since the brief was replacement only. Third Pillar Portal's key-steer line (`#dss-pt-steer`) was marked "keep" in the cuts and is unchanged.
- Numbers: 116 pink divs before and after; the twee lost about 240 lines of draft and gained 175. No link, `(set:)`, `(if:)` or `(link:)` line changed apart from the hand-edited Centre — Nazca hook.
- Verified: nine of the cut passages rendered in Chromium at 1212 with 0 Harlowe errors, 0 JS errors and no overlaps except one on Nazca Approach that pass 1 will pick up (the link now sits 5px under the scene's fixed "STEP DOWN FROM THE BUS" caption because the paragraph above it is shorter). html synced.

## Pass 5 — signposted links inside panes (04:50–04:56 UTC) — DONE, three containers opened to the signpost
- The signpost script (UserScript "SIGNPOSTED CHOICES", commit 43d2513) skips a list of bespoke containers. Three of them held plain navigation links that were still in the old gold-with-left-border style next to the new signposts: `.glass-pane` (The Fetch: "Follow him" and "Not yet. Back to the night."), `.ending-pane` (Alba Complete: "Traveller, wake!"; Alba Incomplete: "Traveller, sleep!") and `.back-to-night-glow` (the span round The Fetch's retreat link, used nowhere else). Those three left the SKIP list; nothing else in the script or the CSS changed. Inside the dark green glass the gold rule and lozenge sit well; the retreat reads as the quiet small-caps street line, which is what it is.
- The glow span's one-off text-shadow pulse no longer shows on that link (the street style sets its own shadow); the span is otherwise harmless and was left in place.
- **Left alone, for Sam:** "PLUS. ULTRA." on the White page and Black page. Those are bare typographic endings (white on white / bone on black, one line fading in at 14s) with their own passage-tag link rules; the gold signpost with its arrow would fight them, so the script still skips the white-page, black-page and dawn-approach tags.
- Screenshots: `pass5-the-fetch-before.png` / `-after.png` / `-after-phone.png`, `pass5-alba-complete-before.png` / `-after.png`, `pass5-alba-incomplete-before.png` / `-after.png` / `-after-phone.png`. (Full-page shots show a pale band below the first 900px; that is the screenshot tool extending the page past the fixed-height backdrop, not something a player sees.)
- Verified: 0 JS errors on The Fetch (17s, so the link gate has opened), Alba Complete, Alba Incomplete at 1212 and 390. html synced.

## Pass 1 — overlapping text (04:38–05:10 UTC, sweep in the background throughout) — DONE, three fixes
- Sweep of all 207 story passages at 1212 and 390 (`overlap_sweep.py`, results `overlap_1212.json` / `overlap_390.json`): 0 Harlowe errors, 0 JS errors. Every flag was looked at; most were things a modal or a scene is meant to cover (the rules card over a minigame heading, the notebook dialog over the stat bars, a 3D approach scene's fixed captions over the prose that scrolls under it, a scrolling map's off-view door label under the map's own caption bar, the stat-delta figure drifting over its label for a second, the worm-game wall's decorative clipping, the tarot cards' 11px face crop, the `.trishas-frame` whose bottom ornament sits in its padding).
- **Fixed 1, Fetch Window SVG** (the "man with your face at the window" pane shown when the dual ring has been refused twice): the caption group `<g … text-anchor="middle">` was closed straight after its `<animate>`, so its four lines lost their centring, italic, size and colour and ran off the right edge of the window, clipped mid-word. Same fault as the Fetch Street SVG fixed last night; the `</g>` now sits after the four `<text>` lines. Before/after: `pass1-fetchwindow-before.png` / `-after.png`. Words untouched.
- **Fixed 2, the scene enter buttons on phones**: ten approach scenes (Pillars, Colony Room, Copper's Lair, O'Flatterly's, Third Pillar, Airport Pub, Nazca, Easter Island, Pyramid, Plain of Chebar) build their ENTER button as a fixed element at `left:50%`, which shrink-wraps to half the viewport, so on a 390px phone "STEP DOWN FROM THE BUS" broke into four lines in a tall box (CLIMB IT had the same fault last night). Those ten buttons now carry `white-space:nowrap`, a `max-width` of the viewport less 16px, and padding and letter-spacing that ease from 36px/4px on a desktop to about 31px/3.5px on a phone via `min()`. Desktop values are unchanged. The other scene buttons already had nowrap. Before/after: `pass1-nazca-phone-before.png` / `-after.png`, `pass1-chebar-phone-after.png`.
- **Fixed 3, Lackland's approach caption on phones**: "LACKLAND'S OFFICE, WARDOUR STREET, SOHO" wrapped to two lines at 390 and its top line ran into the ENTER button 7px above it. The caption is now one line (nowrap, font size and spacing eased with `min()`; unchanged on a desktop). Before/after: `pass1-lacklands-phone-before.png` / `-after.png`. (As noted in pass 2, the caption says Wardour Street while both maps say Frith; wording left alone.)
- One more item from this sweep for pass 6: `html` and `body` have no background of their own (the night gradient is on `tw-story`), so Safari's rubber-band overscroll shows white above and below the page. Fixed in pass 6.
- Verified: `node --check` on the UserScript clean; html synced; the three passages reshot at 390 with 0 errors.

## Pass 6 — visual consistency sweep (05:10–05:15 UTC) — DONE, two repairs
- **Safari overscroll.** `html` and `body` had no background; the night gradient is on `tw-story`. Chromium never shows this, but Safari's rubber-band at the top and bottom of a page reveals the canvas, which was white. Now `html { background: #0a0908 }` (the gradient's own top colour) and, with `:has()`, white again on the white-page ending so that page keeps its white edge. Checked in Chromium: html reads rgb(10,9,8) on Dean Street and rgb(255,255,255) on White page. Safari itself was not run.
- **Reduced-motion hold on the coast of Carthage.** Of the twenty-two three.js scene builders in the UserScript, `buildCaScene` (The coast of Carthage, id `ca-wrap`; last night's log called it the Colony approach, which it is not) was the only one with no `prefers-reduced-motion` handling. It now holds its clock at 40s (camera breath, sun pulse, sea shimmer, reflection sway, fog drift and fire flicker all still) and skips the cloud drift and the rising ember sparks. Everything else in the function is untouched. Renders under reduced motion with 0 errors.
- Looked at and left: the Fetch Street SVG's SMIL animation and 15s gate, and the astral reveal at Centre Point, still ignore reduced motion (flagged for Sam last night; unchanged). Phone overflow: the pass 1 sweep's spill check found no text passing its container at 390 on any passage.

## Pass 4 — play every route (04:55–05:25 UTC, walker in the background) — DONE, nothing broken
Walker: `scratchpad/polish-loop-2026-09-23/routes.py` (results `routes_1212.json`, `routes_390_reduced.json`, `routes_minis.json`, logs `routes*.log`). Each route is seeded straight into its first passage, clicks through with a preference for forward links (or for retreat links on the turn-back routes), forces the minigame outcome by un-hiding the win/lose exit span, and records tw-errors, JS errors, console errors and dead ends at every step.
- **Dream worlds at 1212:** Himalayas, Nazca, Easter Island, Pyramid, Ezekiel, each win / lose / turn-back: 18 routes clean, all back at Dean Street.
- **Nights:** complete night (Alba Complete → white dawn), incomplete night (Alba Incomplete → black dawn), Alt-Dawn: clean at 1212, and again at 390 with reduced motion (all 18 world routes plus the three nights re-run at 390 reduced: clean).
- **Minigames at 1212:** pong win/lose, cow win/lose, bar (Ronnie Scott's) win/lose, fight victory and defeat (defeat walked by hand: Fight Defeat → Beaten → St. John's Word → The dark pass → Dean Street), waltz up/down/onward (the Result passages are pass-throughs to Approach O'Flatterly), House of Cards win/excellent/loss (each returns to LINE 2), worm exit to the Square: clean.
- No broken links, no dead state, no tw-errors, no game JS errors on any route. The only console noise is the sandbox's audio decode failure. Safari not run.

## Pass 7 — spectral touches (05:15 UTC onward) — one at a time, each committed
Rule from Sam: beauty in this world is spectral, the trace, the ghost, the memory. After-images, reflections, faded ink, half seen. Nothing bright, new or ornamental. Every touch here is additive CSS/canvas, reduced-motion safe, and leaves his words alone.
- **7a — the title's after-image** (commit 4ea7cd4). `.game-title` on Dean Street now carries two offset ghost copies of itself in its text-shadow, a cold one up-left and a warm one down-right, at 12–26% alpha, drifting a couple of pixels across the existing eight-second breath. Had to be folded into the `dssTitleBreath` keyframes because that animation owns the text-shadow; shadow counts matched in both keyframes so the copies stay sharp. Before/after `pass7-hubtitle-*.png` (a 3× crop too).
- **7b — ghost ink on the memory photographs** (same commit). `.memory-photo .mp-prose` gains a faint offset double, warm one way and cold the other, like ink printed through from the back of a page. Before/after `pass7-memoryphoto-*.png`.
- **7c — footprints on the Dean Street map** (commit bdf3e08). Each completed step on the tile map leaves a pale 2×2 print on the wet pavement, alternating left and right of the walker's line, that the rain takes back over about seven seconds (quadratic fade from 50% alpha). Drawn in the same camera pass as the walker sprite, before the walker's light so it sits under him. First tint (34%, 1×2) was invisible at play size; the debug run in red proved the placement, then the tint was lifted. Before/after `pass7-footprints-{before,after}-3x.png` (figure walked south down Dean Street). Under reduced motion the prints still fade, which is a change of opacity, not motion.
- **7d — the after-image of the passage just left** (commit db761a0). When a passage goes, an inert copy of it (a clone of the `tw-passage`, links turned to spans, ids dropped, scripts, frames, canvases, the stat bars and anything pinned to the viewport cut) sits where it was, at 42% opacity, lifting seven pixels and fading over 0.95s, while the new passage waits its usual 0.55s before fading in. The two crossfade: the old page is still on the retina as the new one arrives. Placed inside `tw-story` so it inherits the page's type (first version sat in `body` and came out in 16px Times). The rectangle is sampled just before the click (and every 400ms), because a removed node has no geometry. Off entirely under reduced motion; `window.DSS_AFTERIMAGE = false` switches it off. Re-ran five routes (Nazca win, Easter turn-back, complete night, pong, cow) and the hub with it live: clean. Before/after `pass7-afterimage-{before,after}.png` and `-after-phone.png`. **This is the one touch that changes every passage change, so it is the first to look at.**
- **7e — the title screen's after-image** (commit e588823). The same two ghost copies as the Dean Street title, folded into `dst-titleGlow`. Before/after `pass7-titlescreen-{before,after}.png`.
- **Tried and dropped (not committed):** a lingering hover glow on links (the signposted links animate their own glow and the header links have their own hover, so the base rule never showed; reverted); a "door still warm" glow on the Dean Street map at the doorway just left (invisible against the door's own halo; reverted). A wet-pavement reflection under the lamps was already there (halo plus smear), so nothing to add.
- **Regression after the touches (06:00–06:10 UTC):** every desktop route re-walked (28 clean, the waltz/worm flags are the walker's blind spots, hand-walked earlier) and the phone overlap sweep re-run: same flag set as pass 1 except Fetch Window SVG (fixed) gone and Approach Trisha's newly flagged, where a timed hint slip ("You can leave", an opaque paper card, bottom 64px on phones) briefly covers the scene caption; it is a toast covering content by design, like the modals in pass 1, so left alone. Hardened the after-image so no animation or transition replays inside the ghost.
- Also tried and dropped: a wet-pavement reflection of the walker under his feet; the figure is dark on dark, so even at full strength it only reads as a second shadow.
- **7f — the map's ink through the notebook paper** (committed). On every notebook tab except the map, the Dean Street map shows faintly through the page, mirrored as ink does through paper: the street blocks and venue markers at 8.5%, the cartouche, compass, key, street names and background stripped. Built once per notebook from the map's own SVG (cloned, serialised to a data URI) and laid under the panel's ruled lines by the notebook layout script. Static, so nothing to hold under reduced motion. Loud test first (60%) proved placement; before/after `pass7-notebook-bleed-{before,after}.png`, plus `-after-poem.png` and `-after-phone.png` to show the poem page and the phone stay legible.
- **7g — the notebook, closed, stays on the eye** (committed). The after-image now covers the notebook too: when its dialog closes, an inert copy of the page (tabs and panel as they were, links to spans, ids, data attributes and onclick handlers dropped) sits where it was and fades the same 0.95s. Two lookups that could have caught the ghost (`dssRevealPentangleOnMap` and the notebook layout script) now ask for `tw-dialog .nb-page`. Reopening the notebook during the fade works. Before/after `pass7-notebookclose-{before,after}.png`.
- **WebKit note on the after-image:** `tw-story` carries `backdrop-filter`, which in WebKit can make it the containing block for fixed descendants. The ghost now measures itself after insertion and nudges its top/left to the sampled rectangle, so it lands right either way. Safari itself was not run.
- **Regression, second round (06:15–06:32 UTC):** eleven routes re-walked at 390 without reduced motion, so the after-image was live on a phone: clean. Desktop overlap sweep re-run: same flag set as pass 1 except Fetch Window SVG (fixed) gone and Nazca Approach newly listed, where the passage's "Out onto the pampa" link now sits under the scene's STEP DOWN FROM THE BUS button in geometry; the scene covers the whole viewport until that button is pressed (screenshot `regress-nazca-approach-1212.png`), so this is the scene covering by design, as in pass 1. Nothing new broken.
